const fs = require('fs');
const chalk = require('chalk')
const prompts = require('prompts')
module.exports = async (page, print) => {
    print(chalk.green('[+]') + ' Welcome to TikTok Follow Bot .')
    require("process").exit(1)
}